import { Template } from '../types';
import { HeritageShell } from './Shell';

export const heritageTemplate: Template = {
  id: 'heritage',
  name: 'Editorial Heritage',
  subtitle: 'Scholarly Press',
  Shell: HeritageShell,
};

export default heritageTemplate;